Initialize the pinned Wave-MPRAGE and Wave-GRE dependencies after cloning:

```bash
git submodule sync --recursive
git submodule update --init --recursive
```

See `tools/wave_retro_lr_recon/SETUP.md` for the reconstruction environment.
