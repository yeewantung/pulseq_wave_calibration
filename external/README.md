# External dependency

Initialize the pinned upstream repository here:

```bash
git submodule update --init --recursive
```

The retrospective script imports the verified reconstruction helpers from:

```text
external/wave-mprage/recon/
```
